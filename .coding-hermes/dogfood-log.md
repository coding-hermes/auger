# auger dogfood log

| date | verdict | promise | top findings | TTFS | install_seconds |
|---|---|---|---|---|---|
| 2026-09-22 | ✅ SHIPPABLE (with substrate caveat) | "A user can drill a project into a git-backed spec of choices via an 11-verb CLI backed by DuckBrain+JEV" — core loop holds: init/start/ask/answer/check/toggle/dump all correct and fast on the working substrate; the documented fresh-install pairing is broken (README + duckbrain default branch lack the declared-tables API). | AUG-016 (P0 fresh-user pairing broken: duckbrain default branch has no tables API — README must pin feat/native-s3) · AUG-015 (P1 toggle allows two active options per decision; dump renders both as the config) · AUG-017 (P1 live namespace edge declaration drifts from code; cross-project fields silently dropped; one init repairs) | ~90s on control box; fresh box: broken as documented, ~4 min once branch pinned | fresh leg ~240s incl. npm (bunker las-bunker-03, agent cab82db4, destroyed; smoke=ok on branch) |
| 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (graph/bundles/verdict surface) | "A user can run the graph walks (propagate), the feedback engine, bundles (SPEC-002) and the verdict record" — the graph engine, feedback/gate loop and verdict verb WORK and are excellent; the bundle layer is invisible when it runs (silent 0.24-confidence 'unchanged' honored, AUG-027) and half-unreachable (no verb writes the moot trigger, AUG-028; no verb creates membership, AUG-029). | AUG-027 (P1 bundle impact pass silent + no confidence floor) · AUG-028 (P1 moot cascade reachable only by hand-written edges) · AUG-029 (P2 no bundle verbs + scheduler-DB-gated membership + namespace convention documented nowhere) · AUG-030 (P2 no supersession; JEV itself judged the contradictory active config BAD 0.36) · AUG-031 (P2 docs drift: 13-table/11-verb lists miss verdict) · AUG-032 (P3 recall cold 25s vs warm 2s) · AUG-033 (P2 fresh-install: 3 undocumented steps) | first real success (feedback → real question) 17s after setup; setup cost ~10 min incl. a namespace-convention mis-pair the docs caused | fresh leg re-run: auger loop 7s zero-install; substrate 3 undocumented steps then ~85s (bunker las-bunker-03, agent 864a37ab, destroyed; smoke=ok) |
| 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (registers + ask→answer seam) | "A user can record breaks/assumptions/unknowns and complete the README's own ask→answer loop" — read/derive verbs (check/status/dump/verdict/recall/propagate/feedback) are excellent and fast; the WRITE surface breaks the loop: ask persists nothing (AUG-035), answer's refusal leaves half a write behind and the retry duplicates the decision id (AUG-034), toggle/dump help-text contracts lie (AUG-036/037), --domain unvalidated (AUG-038); registers remain verb-less (AUG-019, confirmed twice by use). Fresh-install leg re-run post-AUG-033: README bootstrap accurate, one token gap remains (AUG-039). | AUG-034 (P1 non-atomic refusal + duplicate id) · AUG-035 (P1 ask→answer seam broken) · AUG-036/037/038/039 (P2 contract drifts) | ~12 min to first real verdict (verdict --ask-jev judged my what-if BAD, noul 0.07); setup 3s init + 1s start | fresh leg 72s substrate build + 6s smoke 19/21 (fails=missing OR key); bunker las-bunker-03, agent 4b004b7a, destroyed; smoke=ok |
| 2026-09-23 (4th) | 🟡 PROMISING-BUT-ROUGH (seam live + HTTP layer) | "The documented ask→answer loop completes, and the spec is queryable over HTTP, searchable by embedding, versioned in git" — the loop NOW COMPLETES via feedback→answer (Q-000001 closed live, propagate clean); the AUG-035 ask-store seam itself never fired (5/5 JEV proposals below T_SUBJECT, AUG-041) and stores a criterion slug not a sentence (AUG-042); HTTP reads/POST/DELETE are clean but PATCH can 200-and-write-nothing (AUG-043), keyless POST is accepted (AUG-045), count is ignored, bad filters 500; "versioned in git" is false (AUG-044); embedding store has no HTTP route (AUG-046). | AUG-041 (P1 seam unsatisfiable in practice) · AUG-043 (P2 substrate PATCH 200-noop) · AUG-044 (P2 git-versioning claim false) | loop completion ~12 min from empty namespace; HTTP probe suite ~10 min | control host, no bunker leg this run — explicit SKIPPED row AUG-048 on the board (install path re-proven 3× in 24h by runs 2-3; this run's surfaces were install-independent: CLI verbs + raw HTTP) |
| 2026-09-24 (5th) | 🟡 PROMISING-BUT-ROUGH (real drill judged as a product) | "A user can point auger at a real decision, let it ask what is missing, answer with the alternatives that lost, and get back a configuration another person can read" — the drill WORKS and the output is genuinely useful (`dump --config` even names a contradiction against the record, unprompted); the WRITE surface breaks the last step: `answer` accepts a --chosen that matches no --option and stores a configuration selecting NOTHING while printing success (AUG-055), and the already-answered gate re-proposes the question it just closed, costing a duplicate decision (AUG-056). | AUG-055 (P1 silent empty configuration on mismatched chosen/option) · AUG-056 (P1 gate re-proposes the closed question) · AUG-057 (P2 --why-not keeps only its last value) · AUG-058 (P2 costs/breaks unfillable but rendered) · AUG-059 (P2 status self-contradicts) · AUG-060 (P2 fixed :3000 unsafe on a shared host; got 500s from strangers' daemons) | ~90s empty namespace -> rendered configuration (~17s of it model calls); status 199ms± 12ms, dump 111ms ± 9ms, recall 3.9s, ask 5-6.4s | fresh leg PASSED: substrate 142s + loop 1s on a bare agent (bunker las-bunker-03, agent d1a530b6, destroyed; smoke=ok) |
| 2026-09-24 (6th) | 🟡 PROMISING-BUT-ROUGH (returning-user regression + trust leg) | "A user can come back, check what is already answered, drill further, and trust the namespace survives a substrate restart" — both run-5 P1 fixes verified holding (AUG-055 refuses rc=1 nothing-written; AUG-056 gate blocks the duplicate), full namespace+embeddings survived kill+restart on a dedicated substrate, check/recall/toggle/feedback all exercised; new blocker: `feedback` crashes with raw InvalidURL on any project name containing a space (URL values unencoded). | AUG-061 (P1 unencoded query values crash feedback on spaced names) · AUG-062 (P2 -1.0 confidence sentinel leaks into dump/status/feedback) · AUG-063 (P3 check 6.5-9.7s warm vs dump 0.10s) | ~3 min to rendered config on a fresh namespace; restart survival proven same session | dedicated substrate :3117 no install leg needed (install path re-proven by runs 2/3/5; durability was this run's install-adjacent surface) |

| 2026-09-24 (6th) | install leg | SKIPPED-install-bunker — install path re-proven same-day by run 5 (agent d1a530b6, smoke=ok); this run's fresh surface was durability, proven on a dedicated local substrate (:3117, kill+restart, namespace+embeddings intact). Row AUG-064. |
| 2026-09-24 (7th) | 🟡 PROMISING-BUT-ROUGH (scale + concurrency — the untouched surface) | "A user can grow a real drill to dozens of decisions and the tool stays fast, consistent, and trustworthy — including two users writing the same namespace at once" — writes stay fast and consistent to 100 rows (60 answers/9 s serial, 0.15 s warm each), then the 100-row page cap silently bends every read (AUG-068) and permanently locks all writes past 100 decisions (AUG-069, P0); concurrent writers corrupt duplicate ids into the store and dump renders the collision as a two-valued configuration with no warning (AUG-070). recall scales badly (22 s warm at 61 memories vs 3.9 s small). | AUG-068 (P1 silent 100-row truncation, no pagination in status/dump) · AUG-069 (P0 count-mint freeze = permanent write lockout) · AUG-070 (P1 TOCTOU duplicates render as a real config) | ~90 s empty namespace -> 60-decision drill; first anomaly noticed at answer 60 | install leg SKIPPED-install-bunker explicitly (re-proven same-day by run 5, agent d1a530b6); scale/concurrency is install-independent; off-by-one sub_623674 + sub_a9b935; evidence docs/dogfood/2026-09-24-scale-concurrency-integration.md |
