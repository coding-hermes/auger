diff --git a/.coding-hermes/board/tasks.jsonl b/.coding-hermes/board/tasks.jsonl
index 3c2b06e..8f995b0 100644
--- a/.coding-hermes/board/tasks.jsonl
+++ b/.coding-hermes/board/tasks.jsonl
@@ -43,3 +43,10 @@
 {"id": "AUG-040", "title": "[P2] gitreins tier1 tests step red-by-harness on merged commits: post-GATE TypeError + battery timeout; AUG-028 gitreins task dangles in_progress", "priority": 2, "detail": "Two tier1 verdicts on 2026-09-23 merges are red from the HARNESS, not the code: (1) .gitreins/history/2026-09-23/047fdeed (commit 64e076a): tier1 tests step 'Command timed out' \u2014 the merged-tree battery under concurrent-wave load exceeds the step budget (AUG-021 raised the hook timeout before; this run still timed out). (2) 1987bcbb (commit 8210d38): the guard's post-suite summarizer heredoc crashed 'TypeError: argument of type NoneType is not iterable' (python <stdin> line 70) AFTER the suite itself printed 'passed 21, failed 0' + 'GATE PASS' \u2014 step exit_code=1 -> tier1 red -> verdict passed MISSING -> `gitreins task complete AUG-028` left the task in_progress even though the work is merged and the tree is green (control-host smoke at HEAD this run: 21/21). Fix direction: (a) make the tier1 tests script's summarizer None-tolerant (a post-GATE crash must not flip a green suite red \u2014 read the log's GATE PASS line as authority); (b) re-run tier1 without concurrent waves (scheduling, per AUG-021) or raise the step budget; (c) then complete the AUG-028 gitreins task so the judge verdict lands. Repro: jq '.stages.tier1.summary' .gitreins/history/2026-09-23/{047fdeed,1987bcbb}/verdict.json."}
 {"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": null, "capability_tags": ["release", "versioning", "public-repo"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:05:00.000000+00:00", "depends_on": null, "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": null, "foreman_note": "Filed by the releng satellite sweep 2026-09-23. Evidence commands re-runnable: git ls-remote --tags origin; gh release list --repo coding-hermes/auger; ls CHANGELOG*. This row authorizes NOTHING \u2014 it records the gap. A separate RELEASE-cut row with repo+version is the only cut authorization (releng doctrine).", "guard_result": null, "id": "RELEASE-001", "lines_added": null, "lines_removed": null, "perpetual": null, "primary_model": null, "primary_provider": null, "priority": "P2", "reasoning": "Releng sweep 2026-09-23 (tick auger-releng-2026-09-23-09-39-55), all verified live against origin:\n- `git ls-remote --tags origin` -> EMPTY (no refs/tags/*). `gh release list --repo coding-hermes/auger` -> empty. No `gh release view` target exists. HEAD aef5b80 == origin/main (parity 0).\n- No CHANGELOG.md in the tree; no version constant in auger.py (grep __version__/VERSION = only code comments); no pyproject.toml/setup.py/package.json \u2014 the release surface is bare single-file Python.\n- README.md L10 claims \"Status: v0.1 \u2014 the loop works end to end\". Per releng doctrine a prose status line is a CLAIM, not a RECEIPT: no tag, no Release object, no artifact backs it. A consumer cloning the repo gets whatever HEAD is, with no version identity and no rollback anchor.\nConsumer impact: the repo is PUBLIC (AUG-012 evidence) with its verbs as the contract (AUG-018: verb renames are BREAKING); without a tag there is no way to pin, cite, or roll back a known-good tree, and no semver signal for the breaking-change policy the docs already assume.\nNot a blocker to file-and-forget: this row is the readiness gate. First cut requires, in order: (a) the open release-blocking rows below closed (AUG-035 is P1 and breaks the documented ask->answer loop); (b) a version-prep commit establishing the minimal version surface (a __version__ constant or pyproject, CHANGELOG with Unreleased, README status line tied to it); (c) CI green on the prep sha; (d) annotated tag + Release object with the gate transcript. Semver call belongs to Bane (start at 0.x per README status or 1.0 \u2014 a decision, not a default).", "review_notes": null, "status": "pending", "title": "[P2] release surface is EMPTY: zero git tags, zero GitHub Release objects, no CHANGELOG, no version stamp \u2014 the public repo has no consumable release", "updated_at": "2026-09-23 10:05:00.000000+00:00", "worker_status": "pending", "worker_summary": null, "detail": null, "worktree": null, "branch": null}
 {"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": null, "capability_tags": ["release", "release-blockers", "dogfood"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:05:00.000000+00:00", "depends_on": null, "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": null, "foreman_note": "Filed by the releng satellite sweep 2026-09-23 from a live board parse (all five rows verified status=pending at sweep time). Closes automatically when the named rows close \u2014 re-derive, never carry forward.", "guard_result": null, "id": "RELEASE-002", "lines_added": null, "lines_removed": null, "perpetual": null, "primary_model": null, "primary_provider": null, "priority": "P2", "reasoning": "Releng sweep 2026-09-23 board census (43 rows parsed live): pending rows that block a first release cut per the \"no release over an unacknowledged blocker\" rule:\n- AUG-035 (P1): ask never persists the question it proposes; answer --question-id then refuses; no verb stores questions. The README central loop (ask -> answer) CANNOT be completed by a user following the docs. This is a documented-happy-path break, not a rough edge.\n- AUG-036 (P2): toggle --on <bare-suffix> exits 0 and patches ZERO rows \u2014 silent no-op that looks like a write.\n- AUG-037 (P2): dump --config help advertises \"D-001=O2\"; the parser silently discards that exact shorthand at rc=0 \u2014 the rendered hypothesis is silently the CURRENT config, not the requested one.\n- AUG-038 (P2): answer --domain accepts any string (4.99 recorded silently); status domain display one-index-off \u2014 the number a user copies is wrong twice.\n- AUG-039 (P2): README token claim false on a fresh box; first auger command dies \"no DuckBrain token\" following the README verbatim.\nConsumer impact: every one of these sits on the first-hour path of a fresh user of a PUBLIC repo (AUG-016/AUG-033 class evidence: fresh installs are actively being tested). Cutting v0.1 with AUG-035 open would ship a README whose core loop is broken.\nThis row is the release-risk flag, not new work: the fixes belong to the named AUG rows on this same board. RELEASE-001 tracks the version surface; this row tracks that a cut waits on these closures (or an explicit waiver in the release row).", "review_notes": null, "status": "pending", "title": "[P2] release blockers open at sweep time: AUG-035 P1 breaks the documented ask->answer loop; AUG-036/037/038/039 P2 silent no-ops + false docs on the fresh-user path", "updated_at": "2026-09-23 10:05:00.000000+00:00", "worker_status": "pending", "worker_summary": null, "detail": null, "worktree": null, "branch": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "Live drive 2026-09-23 (ns dogfood-auger-20260923e, real micro-spec, JEV live): five `ask` calls proposed new_question confidences 0.23/0.19/0.24/0.17/0.18 \u2014 every one below T_SUBJECT=0.45, so `store_proposed_question` refused every store (printed correctly, fail-closed). The seam the fix exists to provide has not produced a single stored question in real use; the only working question path is feedback\u2192answer (verified live: Q-000001 closed by answer --question-id). Repro: any seeded project with >=2 confident decisions, run `ask` repeatedly. Fix direction: (a) measure T_SUBJECT against live JEV score distributions for new_question (the criterion is 'which question next', whose confidence may be structurally low, unlike an answered-score); (b) consider storing below-threshold proposals as a lower qclass with a visible marker instead of refusing; (c) or lower the gate for ask_proposed rows only. The current combination makes AUG-035's benefit unreachable in practice.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-041", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 1, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P1] ask\u2192store seam (AUG-035) never fires in live use: 5/5 JEV proposals below T_SUBJECT (0.15-0.24 vs 0.45)", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "In every live `ask` call (2026-09-23 4th dogfood) the 'next question' line printed the criterion slug `how_does_it_fail` (0.23/0.19/0.24...), never a sentence. Had any proposal passed the T_SUBJECT gate (AUG-041), the SLUG would be the stored question text a user then answers with --question-id \u2014 a meaningless question on the record. Repro: `ask` twice on logtriage namespace; 'next question: how_does_it_fail'. Fix direction: either prompt JEV to emit the question TEXT in new_question (the criteria set already names the five candidate questions \u2014 ask for the text), or map slug\u2192canned sentence before storing.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-042", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P2] ask's proposed question is the JEV criterion SLUG, not a question sentence", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["substrate", "duckbrain"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "HTTP integrator probe 2026-09-23 (ns dogfood-auger-20260923e): PATCH /api/ns/<ns>/tables/decision?pk=eq.D-002 body {confidence:0.88} -> 200; immediate readback shows confidence 0.82 UNCHANGED and the disk JSONL never carried 0.88. Same class as AUG-036 (toggle silent no-op) but in the substrate: a write whose only signal is a status code cannot be trusted. auger's toggle path is exposed to this. Fix: DuckBrain PATCH must either write or return a real error; short-term, integrators (and SKILL.md, already updated) must read-back-verify every PATCH. Evidence: /tmp probe transcript in docs/dogfood/2026-09-23-seam-http-integration.md.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-043", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P2] DuckBrain PATCH ?pk= returns 200 and writes NOTHING (silent no-op one layer below auger)", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["docs", "substrate"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "README L132 + DESIGN L103-107 claim the spec is 'versioned in git'. Verified 2026-09-23: ~/duckbrain/.gitignore root-anchors /namespaces/ (checked-ignore hit .gitignore:13), the JSONL is untracked, and no snapshot/commit machinery runs (_audit/current.jsonl is an append audit log, not versioning). A user checking git status sees their spec is NOT in git. Same probe also found: (a) GET tables/<t>?count=exact / ?count -> 200 with NO Content-Range and no count anywhere \u2014 pagination params silently ignored; (b) GET tables/<t>?confidence=gt.banana -> 500 INTERNAL_ERROR instead of a 400 naming the bad value. Fix direction: README/DESIGN wording until versioning actually exists (or wire the duckbrain snapshotter); DuckBrain: honor count, 400 on bad-typed filters.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-044", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P2] README/DESIGN 'versioned in git' is FALSE as shipped: duckbrain gitignores /namespaces/; also count params ignored + bad typed filter 500s", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["substrate", "duckbrain"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "HTTP integrator probe 2026-09-23: POST /api/ns/<ns>/tables/question [{project_id:...}] (no id) -> 201 {inserted:1}; row stored with id:null (declared primary key). Consequence: unreachable \u2014 every PATCH/DELETE requires ?pk=eq.<value> (400 otherwise) and there is no value; ?id=is.null DELETE is refused. Only escape was hand-editing question.jsonl, which violates auger's own no-direct-edit rule. Fix: DuckBrain \u2014 enforce/require the declared primary key on POST (400 on missing/empty id); auger-side: nothing (the CLI always sends ids). Evidence: docs/dogfood/2026-09-23-seam-http-integration.md.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-045", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P2] DuckBrain POST accepts a row with NO id (201) -> id:null row unreachable via API, only hand-editable", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["substrate", "duckbrain", "docs"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "README's data-location section implies the spec is queryable/searchable over HTTP. The embeddings (memories) are NOT exposed: GET /api/ns/<ns>/memories -> 404 ROUTE_NOT_FOUND; 'memories' is not among the 14 declared tables. Semantic search is reachable only via auger's recall/check. An integrator building on the HTTP API cannot search. Fix: expose memories as a declared table or a /search route on the substrate; or scope the README claim to 'queryable over HTTP; searchable via auger recall'. Evidence: probe transcript in docs/dogfood/2026-09-23-seam-http-integration.md.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-046", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P2] embedding store has NO HTTP route: 'searchable by embedding' (README) only true through auger verbs", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["cli", "perf"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 10:45:00", "depends_on": [], "detail": "hyperfine 2026-09-23 (quiet-ish box, fleet load): dump 191ms +- 196ms (min 93ms, 10 runs); status 2.17s +- 1.28s (range 1.1-4.3s, 10 runs) \u2014 variance is real load, not noise, but the 10x gap is structural (status walks all 44 domain rows + question/edge tables). Recorded as a number, NOT a perf row: at this namespace size a user would not care; revisit if status ever matters interactively. Command: hyperfine --warmup 2 --runs 10 'python3 auger.py -n <ns> status'.", "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": null, "guard_result": null, "id": "AUG-047", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 3, "reasoning": "dogfood 2026-09-23 (4th run): live seam drive + HTTP integrator probe", "review_notes": null, "status": "pending", "title": "[P3] status is ~10x slower than dump (2.2s vs 0.19s warm, sigma 1.3s) on a 4-decision namespace", "updated_at": "2026-09-23 10:45:00", "worker_status": null, "worker_summary": null}
diff --git a/.coding-hermes/dogfood-log.md b/.coding-hermes/dogfood-log.md
index 975f596..a93605f 100644
--- a/.coding-hermes/dogfood-log.md
+++ b/.coding-hermes/dogfood-log.md
@@ -5,3 +5,4 @@
 | 2026-09-22 | ✅ SHIPPABLE (with substrate caveat) | "A user can drill a project into a git-backed spec of choices via an 11-verb CLI backed by DuckBrain+JEV" — core loop holds: init/start/ask/answer/check/toggle/dump all correct and fast on the working substrate; the documented fresh-install pairing is broken (README + duckbrain default branch lack the declared-tables API). | AUG-016 (P0 fresh-user pairing broken: duckbrain default branch has no tables API — README must pin feat/native-s3) · AUG-015 (P1 toggle allows two active options per decision; dump renders both as the config) · AUG-017 (P1 live namespace edge declaration drifts from code; cross-project fields silently dropped; one init repairs) | ~90s on control box; fresh box: broken as documented, ~4 min once branch pinned | fresh leg ~240s incl. npm (bunker las-bunker-03, agent cab82db4, destroyed; smoke=ok on branch) |
 | 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (graph/bundles/verdict surface) | "A user can run the graph walks (propagate), the feedback engine, bundles (SPEC-002) and the verdict record" — the graph engine, feedback/gate loop and verdict verb WORK and are excellent; the bundle layer is invisible when it runs (silent 0.24-confidence 'unchanged' honored, AUG-027) and half-unreachable (no verb writes the moot trigger, AUG-028; no verb creates membership, AUG-029). | AUG-027 (P1 bundle impact pass silent + no confidence floor) · AUG-028 (P1 moot cascade reachable only by hand-written edges) · AUG-029 (P2 no bundle verbs + scheduler-DB-gated membership + namespace convention documented nowhere) · AUG-030 (P2 no supersession; JEV itself judged the contradictory active config BAD 0.36) · AUG-031 (P2 docs drift: 13-table/11-verb lists miss verdict) · AUG-032 (P3 recall cold 25s vs warm 2s) · AUG-033 (P2 fresh-install: 3 undocumented steps) | first real success (feedback → real question) 17s after setup; setup cost ~10 min incl. a namespace-convention mis-pair the docs caused | fresh leg re-run: auger loop 7s zero-install; substrate 3 undocumented steps then ~85s (bunker las-bunker-03, agent 864a37ab, destroyed; smoke=ok) |
 | 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (registers + ask→answer seam) | "A user can record breaks/assumptions/unknowns and complete the README's own ask→answer loop" — read/derive verbs (check/status/dump/verdict/recall/propagate/feedback) are excellent and fast; the WRITE surface breaks the loop: ask persists nothing (AUG-035), answer's refusal leaves half a write behind and the retry duplicates the decision id (AUG-034), toggle/dump help-text contracts lie (AUG-036/037), --domain unvalidated (AUG-038); registers remain verb-less (AUG-019, confirmed twice by use). Fresh-install leg re-run post-AUG-033: README bootstrap accurate, one token gap remains (AUG-039). | AUG-034 (P1 non-atomic refusal + duplicate id) · AUG-035 (P1 ask→answer seam broken) · AUG-036/037/038/039 (P2 contract drifts) | ~12 min to first real verdict (verdict --ask-jev judged my what-if BAD, noul 0.07); setup 3s init + 1s start | fresh leg 72s substrate build + 6s smoke 19/21 (fails=missing OR key); bunker las-bunker-03, agent 4b004b7a, destroyed; smoke=ok |
+| 2026-09-23 (4th) | 🟡 PROMISING-BUT-ROUGH (seam live + HTTP layer) | "The documented ask→answer loop completes, and the spec is queryable over HTTP, searchable by embedding, versioned in git" — the loop NOW COMPLETES via feedback→answer (Q-000001 closed live, propagate clean); the AUG-035 ask-store seam itself never fired (5/5 JEV proposals below T_SUBJECT, AUG-041) and stores a criterion slug not a sentence (AUG-042); HTTP reads/POST/DELETE are clean but PATCH can 200-and-write-nothing (AUG-043), keyless POST is accepted (AUG-045), count is ignored, bad filters 500; "versioned in git" is false (AUG-044); embedding store has no HTTP route (AUG-046). | AUG-041 (P1 seam unsatisfiable in practice) · AUG-043 (P2 substrate PATCH 200-noop) · AUG-044 (P2 git-versioning claim false) | loop completion ~12 min from empty namespace; HTTP probe suite ~10 min | control host, no bunker leg this run (install path re-proven 3× in 24h by runs 2-3; explicit SKIPPED not filed because this run exercised install-independent surfaces: CLI verbs + raw HTTP; substrate install evidence remains run-3's 72s build + 6s smoke) |
diff --git a/auger.py b/auger.py
index 6c90833..1590415 100755
--- a/auger.py
+++ b/auger.py
@@ -2850,6 +2850,87 @@ def cmd_start(a):
     return 0
 
 
+# ---------------------------------------------------------------- the ask -> store seam (AUG-035)
+# The README's loop is `ask` -> `answer --question-id`: ask names the question worth drilling and the
+# user records the decision that answers it. Until this seam existed the proposal lived ONLY as a
+# printed line — the `question` table stayed empty after a call that named a next question, so the
+# loop's central move (`answer --question-id Q-...`) refused the engine's own suggestion, and no verb
+# a README reader can run could store a question at all. `feedback` was the only writer of question
+# rows, and it drills THIN DECISIONS (< T_CONFIDENT): a project whose decisions are all confident
+# could not put one question on the record.
+#
+# What is stored is the proposal the user just read, plus the two JEV values the `question` table
+# DECLARES columns for:
+#   * `jev_already_answered` — the gate's noul for exactly this question, which is the field's own
+#     meaning and the reason `propagate` does not re-gate a row carrying it. NOT the new_question
+#     choice's confidence: that number answers "how sure is JEV about WHICH question to ask", and a
+#     reader taking it for an answered-score would see a 0.73 proposal as an answered question.
+#   * `jev_checked_at` — when that verdict was taken.
+# Nothing else is invented. No `opens` edge: the proposal names no decision it came from, and `edge`
+# refuses a claim about a node that is not stored rather than storing an orphan. No `domain` either:
+# JEV picks a SUBJECT, and which of the 44 grid domains it lands in is not something this verb knows.
+ASK_CLASS = "ask_proposed"  # the `qclass` a question proposed by `ask` carries (its source marker)
+
+
+def store_proposed_question(ns: str, project_id: str, nq: dict, noul) -> tuple[str, str]:
+    """Persist the question `ask` proposed. Returns (qid, why); an empty qid means nothing was stored.
+
+    Why this is a function and not four lines inside `cmd_ask`: every reason NOT to store has to be
+    provable on its own — a proposal below T_SUBJECT, a question the gate already answered, a question
+    already open, a refused insert — and each of them must leave the store untouched.
+
+    FAIL-CLOSED, in three parts:
+      * nothing is written until every gate has passed, so a failure before the first write returns
+        "" with the reason and the `question` table keeps exactly the rows it had;
+      * an absent verdict is NOT a "genuinely new" one: a noul the model did not return refuses the
+        store, because a question whose answered-score nobody knows is a question nobody scored;
+      * the row and its facets are separate writes, so a failure AFTER the row landed is returned as
+        a PARTIAL store (both a qid and a reason) instead of being reported as a clean one.
+    """
+    text = str((nq or {}).get("choice") or "").strip()
+    if not text:
+        return "", "JEV proposed no question"
+    conf = (nq or {}).get("confidence")
+    if not isinstance(conf, (int, float)) or float(conf) < T_SUBJECT:
+        return "", f"the proposal is below threshold {T_SUBJECT} (confidence {conf})"
+    if noul is None:
+        return "", "the gate returned no answered-verdict for it"
+    if float(noul) >= T_ANSWERED:
+        return "", f"the gate scores it already answered (noul {float(noul)})"
+    try:
+        open_rows = select(
+            ns, "question", f"project_id=eq.{project_id}&status=eq.open"
+        )
+    except SystemExit as exc:
+        return "", f"the questions already open could not be read — {exc}"
+    want = text.casefold()
+    for q in open_rows:
+        if str(q.get("text") or "").strip().casefold() == want:
+            return "", f"already open as {q.get('id')}"
+    qid = next_id(ns, "question", "Q")
+    row = {
+        "id": qid,
+        "project_id": project_id,
+        "domain": "",
+        "text": text,
+        "ring": 1,
+        "qclass": ASK_CLASS,
+        "status": "open",
+        "jev_already_answered": float(noul),
+        "jev_checked_at": datetime.now(timezone.utc).isoformat(),
+    }
+    try:
+        insert(ns, "question", row)
+    except SystemExit as exc:
+        return "", f"the question row was refused — {exc}"
+    for name in facet_set():
+        try:
+            facet(ns, project_id, qid, name)
+        except SystemExit as exc:
+            return qid, f"the question row landed but facet {name!r} did not — {exc}"
+    return qid, ""
+
+
 def cmd_ask(a):
     """Surface the next questions: JEV's pick, plus every low-confidence decision."""
     ns, pid = a.namespace, a.project_id
@@ -2952,6 +3033,16 @@ def cmd_ask(a):
         if (already or 0) >= T_ANSWERED
         else "genuinely new, ask it",
     )
+    # AUG-035: the proposal becomes a ROW here, which is the whole difference between a report and a
+    # question a reader can close. One added line either way — the question is on the record, or the
+    # reason it is not (fail-closed: no half-stored question, and no exit code of its own).
+    stored_qid, store_why = store_proposed_question(ns, pid, nq, already)
+    if stored_qid:
+        print(f"stored question: {stored_qid}")
+        if store_why:
+            print(f"  WARNING {store_why}")
+    else:
+        print(f"question not stored: {store_why}")
     print(f"\njev cost: {ans.get('usage', {}).get('cost')}  build: {ans.get('model')}")
     return 0
 
diff --git a/docs/VERBS.md b/docs/VERBS.md
index c825934..1d704c1 100644
--- a/docs/VERBS.md
+++ b/docs/VERBS.md
@@ -73,11 +73,23 @@ Surface the next questions: JEV's pick, plus every low-confidence decision.
 
 **Arguments:** none.
 
-**Writes:** nothing. It reads the project's decisions, unknowns, and open questions,
-retrieves related evidence, makes one JEV call, and prints the report.
-
-**Never writes:** anything, anywhere — including on a JEV failure (fail-closed; it
-prints and exits non-zero). `ask` is a report, not a write.
+**Writes:** one `question` row for the question JEV picks — `status=open`,
+`qclass=ask_proposed`, `ring=1`, the proposal's text, and the two JEV values the table
+declares columns for: `jev_already_answered` (the gate's noul for exactly that question)
+and `jev_checked_at` — plus a full set of `facet` rows for it. Its id follows the table's
+own convention (`next_id`: `Q-000001`), and it is printed as `stored question: <id>`. That
+row is what makes the next move in the README's loop (`answer --question-id Q-...`) a real
+one: before it, ask named a question no verb could store.
+
+**Never writes:** a question when the proposal is below `T_SUBJECT`, when the gate scores
+it already answered (`noul >= T_ANSWERED`), when the gate returns no verdict for it
+(unknown is not "genuinely new"), or when the same text is already open in this project
+(said out loud, not stored twice). No decision, no option, no edge, no memory entry: an
+`opens` edge would name a decision the proposal does not come from. A refused row insert
+stores nothing (fail-closed); a failure after the row landed is reported as a partial
+store instead of being printed as a clean one. `ask` stays a report — exit 0 for every
+storage outcome, and exit non-zero only when JEV itself is unreachable (`JEV unavailable`,
+nothing stored).
 
 ## answer
 
diff --git a/docs/dogfood/2026-09-23-seam-http-integration.md b/docs/dogfood/2026-09-23-seam-http-integration.md
new file mode 100644
index 0000000..55a0801
--- /dev/null
+++ b/docs/dogfood/2026-09-23-seam-http-integration.md
@@ -0,0 +1,116 @@
+# auger dogfood integration report — 2026-09-23 (4th run: the ask→answer seam live, and the HTTP data layer as an integrator)
+
+Two untouched angles after three prior runs: (a) the AUG-035 seam fix was in the
+tree uncommitted — this run drove the loop the seam unblocks LIVE, end to end;
+(b) no run had consumed auger's data through DuckBrain's HTTP API the way an
+integrator would — the README's promise "queryable over HTTP, and searchable by
+embedding … the spec is versioned in git" had never been user-tested.
+
+## The run
+
+Scratch namespace `dogfood-auger-20260923e`, control host, live DuckBrain
+(apikey auth) + live JEV. A real micro-spec: a fleet log-triage CLI (retention,
+state store, alerts channel, error codes).
+
+- init --seed-domains: 0.3s, 14/14 tables, 44/44 domains.
+- start: 0.08s. answer ×2: 0.1s each. status: 2.2s warm (σ 1.3 — see costs).
+- ask: 6-8s, fail-closed store refusal printed correctly.
+- feedback: proposed a question from the thin decision D-003 (0.3), JEV refused
+  it as already-answered (noul 0.68) — and feedback RECORDED the refusal as a
+  question row (Q-000001, qclass follow_up). Edges E-000001/2 written.
+- answer --question-id Q-000001: worked — `closed Q-000001` printed, status
+  updated, propagate clean. **The README's central loop now completes by hand.**
+
+### The ask→answer seam itself (AUG-035, live)
+
+Ran `ask` five times across the session. EVERY proposal was refused by the
+gate: confidences 0.23/0.19/0.24/0.17/0.18, all below T_SUBJECT=0.45. The
+fail-closed behavior is exactly to spec and each refusal prints its reason —
+but the practical result is that **the seam the fix exists to provide has
+never fired in live use**: the only JEV-proposed question that ever reached the
+table in this run came from `feedback`, not `ask`. The proposed "question" is
+also just the criterion slug (`how_does_it_fail`), not a sentence — had it
+passed the gate, a slug would be the stored text. Findings AUG-041/AUG-042
+below.
+
+## The HTTP data layer (integrator view)
+
+Written against `~/duckbrain` feat/native-s3, token auth, standard urllib —
+no auger code, following only the README's data-location section:
+
+- GET /api/ns/<ns>/tables → 200, full declared schema, 14 tables. ✅
+- GET /tables/<t>?id=eq.D-001 / confidence=gt.0.85&order=confidence.desc /
+  ring_floor=gte.5&order=num.asc&limit=5 → all 200, correct rows. ✅
+- `text=like.%25canonical%25` LIKE filter → 200, correct. ✅
+- POST question row → 201 `{"inserted":1}`; DELETE ?pk= → 200; readbacks
+  correct; DELETE then re-read clean. ✅
+- PATCH ?pk=eq.D-002 → 200 … **but the row was unchanged** (0.82 stayed 0.82;
+  the disk JSONL never carried 0.88). A 200 on a write that wrote nothing —
+  the same silent-no-op class as AUG-036, one layer down. AUG-043.
+- `count=exact` / `count` params → 200 but **no Content-Range and no count
+  anywhere in the body**; pagination params silently ignored. AUG-042.
+- PATCH/DELETE without `?pk=` → 400 with a precise error naming the required
+  filter shape. ✅ (good error design)
+- GET bad-typed filter (`confidence=gt.banana`) → **500 INTERNAL_ERROR**, not
+  400 with a message. AUG-044.
+- POST a row with NO `id` → **201 accepted**, row stored with
+  `id: null` — and the namespace's declared primary key is `id`. A keyless row
+  is then unreachable through the API (every PATCH/DELETE requires `pk=eq.<v>`
+  and there is no value) and had to be removed by editing the JSONL directly.
+  AUG-045.
+- The embedding store is NOT reachable over HTTP: `/memories` → 404
+  ROUTE_NOT_FOUND and `memories` is not among the 14 declared tables. So the
+  README's "searchable by embedding" is true only through auger's own verbs,
+  not through the substrate's HTTP API an integrator would reach for. AUG-046.
+
+### The git-versioned promise is false as shipped
+
+README: "the spec is **versioned in git**, readable as plain files, queryable
+over HTTP, and searchable by embedding — and the dump is a view of it." Verified
+against the substrate repo: `namespaces/` is **gitignored in duckbrain itself**
+(`/namespaces/`, root-anchored), the JSONL is not tracked, and there is no
+snapshot/commit machinery running (`_audit/current.jsonl` is an append audit
+log, not versioning). Nothing about auger's storage of record is in git. The
+claim may describe the DuckBrain S3 branch's design intent, but a user checking
+`git status` in ~/duckbrain sees their spec is untracked local files. AUG-044
+(class: promise-vs-reality, docs).
+
+## Costs measured (hyperfine, quiet-ish box, σ high from fleet load)
+
+- `auger dump`: 191ms ± 196ms warm (min 93ms); `status`: 2.2s ± 1.3s warm —
+  `status` is ~10× dump and the variance (1.1-4.3s) is real, not noise, on a
+  4-decision namespace. Not worth a PERF row (a coverage report every few
+  seconds is fine), recorded here as the number.
+- `ask`: 6.5-8s per call (JEV round-trip, ~$0.00004); `check`: 1.7-14.2s
+  (embedding + JEV); `recall` warm: 0.6-1.6s; `feedback`: 8.7s.
+- HTTP probes: all sub-50ms.
+- Cold-start re-check: no cold penalty found beyond fleet-load variance
+  (AUG-032 recall cold-start row remains the only perf finding worth a row).
+
+## Verdict
+
+🟡 PROMISING-BUT-ROUGH — narrowed this run to 🟢-leaning on the CLI: the
+documented ask→answer loop now COMPLETES (feedback → question row → answer
+--question-id → closed, propagate clean), the read/derive verbs are excellent
+and fast, and the verdict register is genuinely useful (JEV judged my what-if
+BAD twice, on the record, with reasons). What keeps it rough: the AUG-035 seam
+itself never fires live (every JEV proposal below T_SUBJECT, and the proposal
+is a criterion slug, not a question), and the substrate layer under the
+README's own promises has silent no-ops (PATCH 200-noop), 500s on bad typed
+filters, keyless-row acceptance, no count/pagination, no HTTP path to the
+embedding store — and "versioned in git" is false as shipped.
+
+## Findings filed
+
+- AUG-041 (P1): ask→store seam never fires live — 5/5 proposals below T_SUBJECT
+  (0.15-0.24 vs 0.45); the fix's own gate may be unsatisfiable in practice.
+- AUG-042 (P2): ask stores the JEV criterion SLUG, not a question sentence.
+- AUG-043 (P2): DuckBrain PATCH ?pk= returns 200 and writes NOTHING.
+- AUG-044 (P2): README/DESIGN "versioned in git" is false — namespaces/ is
+  gitignored in duckbrain; bad typed filter → 500 INTERNAL_ERROR.
+- AUG-045 (P2): POST without id accepted (201) → keyless row unreachable via
+  API (no pk), only fixable by hand-editing JSONL.
+- AUG-046 (P2): embedding store not exposed over HTTP ("searchable by
+  embedding" only via auger verbs).
+- AUG-047 (P3): status ~10× slower than dump (2.2s vs 0.19s warm, high σ);
+  number recorded, not a perf row.
\ No newline at end of file
diff --git a/docs/dogfood/diagnostics.md b/docs/dogfood/diagnostics.md
index 6d2943e..2bf40a7 100644
--- a/docs/dogfood/diagnostics.md
+++ b/docs/dogfood/diagnostics.md
@@ -144,13 +144,16 @@ How the write surface actually behaves, as learned by driving a real drill
   but the order is wrong: a "refused" exit must mean nothing was stored.
   Because decision ids are not unique-keyed, the natural retry then created a
   duplicate id (dump rendered it twice; every derived count ×4).
-- **`ask` is a proposal, not a write.** Nothing it prints is persisted (the
-  `question` table stayed empty after a call that named a next question). The
-  only question-writer is `feedback` (thin decisions only), so the README's
-  own loop — ask, then answer `--question-id` — cannot complete for a
-  not-yet-stored question. Anyone reading `ask`'s output as "the engine
-  recorded the next question" is being set up for the non-atomic refusal
-  above.
+- **`ask` was a proposal, not a write (AUG-035; run 2026-09-23).** Nothing it
+  printed was persisted (the `question` table stayed empty after a call that
+  named a next question). The only question-writer was `feedback` (thin
+  decisions only), so the README's own loop — ask, then answer
+  `--question-id` — could not complete for a not-yet-stored question. Anyone
+  reading `ask`'s output as "the engine recorded the next question" was being
+  set up for the non-atomic refusal above. Closed by AUG-035: `ask` now stores
+  the question it proposes (one `question` row with `qclass=ask_proposed` plus
+  its `facet` rows, printed as `stored question: <id>`), and `answer` checks
+  `--question-id` before it writes anything.
 - **Two CLI contracts lie in their own help text.** `toggle --on O2` (bare
   id) exits 0 with "toggled … (0)" and patches zero rows; `dump --config
   D-001=O2` (the form `--help` shows) is discarded as unparsed. The full
@@ -172,3 +175,41 @@ How the write surface actually behaves, as learned by driving a real drill
   (nothing written, contradictions quoted from the record), verdict's
   record-then-judge flow, and the smoke suite's own hygiene (namespace
   sweep/teardown ran clean on the bunker).
+
+## The HTTP data layer + the seam in live use (2026-09-23, fourth run —
+report: docs/dogfood/2026-09-23-seam-http-integration.md)
+
+The substrate's declared-table API, driven by a plain urllib integrator
+(auger's own client code untouched), is mostly excellent and sharp in exactly
+four places:
+
+- **A 200 is not a write.** `PATCH ?pk=eq.D-002` returned 200 and the row was
+  byte-identical afterward (confidence stayed 0.82); the disk JSONL never
+  changed. This is AUG-036's silent-no-op class one layer BELOW auger — any
+  integrator (and `toggle`'s PATCH path) must read the row back after a
+  PATCH, never trust the status code (AUG-043).
+- **The primary key is not enforced on POST.** A row POSTed without `id` is
+  accepted 201 and stored with `id: null` — permanently unreachable (every
+  PATCH/DELETE requires `pk=eq.<value>`, and there is no value). Removed by
+  hand-editing the JSONL, which is the exact thing auger's own rules forbid
+  (AUG-045).
+- **"Versioned in git" is currently false.** duckbrain root-gitignores
+  `/namespaces/` — the storage of record is untracked local files, no
+  snapshot/commit machinery runs (the `_audit/current.jsonl` append log is an
+  audit trail, not versioning). README + DESIGN both claim git versioning
+  (AUG-044). Related: `count`/pagination params 200 with no Content-Range,
+  and a bad-typed filter value 500s instead of 400 (same row).
+- **The embedding store has no HTTP route.** `/memories` → 404 and it is not
+  one of the 14 declared tables; "searchable by embedding" is true only
+  through auger's verbs (AUG-046).
+
+The AUG-035 seam itself, driven live five times: **it never fired.** Every
+JEV proposal scored 0.15-0.24 against T_SUBJECT=0.45 (fail-closed refusals,
+each printed with its reason — correct behavior), and the proposal text is
+the criterion slug (`how_does_it_fail`), not a question sentence (AUG-041/
+AUG-042). The loop the seam unblocks DOES complete end to end by the other
+path, proven live: `feedback` proposed a question from a thin decision
+(recorded as Q-000001 even when JEV refused it, qclass follow_up) →
+`answer --question-id Q-000001` → `closed Q-000001`, status coverage updated,
+propagate clean. Practical rule until AUG-041 is fixed: the question path is
+feedback → answer, not ask → answer.
diff --git a/skills/auger-usage/SKILL.md b/skills/auger-usage/SKILL.md
index 8a8631d..3771063 100644
--- a/skills/auger-usage/SKILL.md
+++ b/skills/auger-usage/SKILL.md
@@ -74,6 +74,27 @@ only. Public repo: `coding-hermes/auger`.
 6. **Shared-host pidfile clash**: `EACCES /tmp/duckbrain-http.pid` = another
    user's daemon; use a different port and `DUCKBRAIN_DATA_DIR`.
 
+## The HTTP data layer (integrator surface, probed 2026-09-23)
+
+Auger's rows are reachable WITHOUT auger: `GET/POST /api/ns/<ns>/tables/<t>`,
+`PATCH/DELETE /api/ns/<ns>/tables/<t>?pk=eq.<id>`, filters `eq/neq/gt/gte/like`,
+`order=<col>.asc|.desc`, `limit=`. Auth = `x-api-key`. Working recipes and the
+sharp edges (all hit live, see docs/dogfood/2026-09-23-seam-http-integration.md):
+
+- ✅ eq/gt/order/limit/LIKE reads, POST (201), DELETE by pk — all clean; a
+  missing `pk` on PATCH/DELETE 400s with a precise message.
+- ⚠️ PATCH can 200 WITHOUT writing (AUG-043): read the row back after any
+  PATCH; never trust the status code.
+- ⚠️ POST with no `id` is ACCEPTED (201) and stores `id: null` — unreachable
+  afterward (AUG-045). Always send a full row.
+- ⚠️ `count`/pagination params are silently ignored (no Content-Range);
+  filter+count client-side (AUG-042/046 family).
+- ⚠️ A bad-typed filter value (`confidence=gt.banana`) 500s (AUG-044).
+- ⚠️ The embedding store (`memories`) has NO HTTP route — semantic search is
+  only reachable through auger's `recall`/`check` verbs.
+- ⚠️ "Versioned in git" (README/DESIGN) is currently FALSE: duckbrain
+  gitignores `/namespaces/` (AUG-044). Treat the JSONL as local files.
+
 ## Quick sanity check (30 seconds)
 
 ```bash
@@ -92,12 +113,26 @@ python3 auger.py -n <ns> recall "any seed phrase"   # proves embeddings
   only `answer --invalidates` (edges, not rows) and the propagate impact
   pass (escalations) write anything. A seed that says "X is unknown" will
   still show `unknowns: 0` until a row is hand-POSTed.
-- **`ask` does NOT persist its proposal (AUG-035).** The question it names
-  is never stored; `answer --question-id Q-…` on it will refuse (AUG-034's
-  non-atomic shape: the DECISION is stored BEFORE the refusal — do not
-  blindly retry without `--question-id`, or you duplicate the decision id;
-  decision ids are not unique-keyed yet). Until fixed: questions enter the
-  store only via `feedback` (thin decisions) or hand-POST.
+- **`ask` persists the question it proposes now (AUG-035 merged 2026-09-23).**
+  When JEV proposes a question above `T_SUBJECT` that the gate scores NEW,
+  `ask` stores it — one `question` row (`status=open`, `qclass=ask_proposed`,
+  the gate's noul in `jev_already_answered`) plus its `facet` rows — and
+  prints `stored question: <id>`. `answer --question-id Q-…` on the line ask
+  just printed therefore works, as the README's loop says. The store is
+  fail-closed: a proposal below `T_SUBJECT`, one the gate scores answered, one
+  with no verdict at all, and one whose text is already open all store NOTHING
+  and print why. Questions still enter the store via `feedback` (thin
+  decisions) when ask proposes no new one.
+  FIELD REALITY (2026-09-23 4th dogfood): across five live `ask` calls every
+  JEV proposal landed below T_SUBJECT (0.15-0.24 vs 0.45) — the seam did not
+  fire once (AUG-041), and the proposal text is the criterion SLUG
+  (`how_does_it_fail`), not a sentence (AUG-042). Do not build on `ask`
+  storing anything yet; the proven question path is `feedback` → question row
+  → `answer --question-id` (verified live: `closed Q-000001`).
+- **`answer` validates its preconditions BEFORE it writes (AUG-034 merged
+  2026-09-23).** A nonexistent `--question-id` (and a duplicate `--id`) now
+  refuses with nothing stored, so the retry that used to duplicate a decision
+  id is gone; decision ids are still not unique-keyed in the store itself.
 - **`--domain` is unvalidated free text (AUG-038)** and `status`'s
   domain-coverage display is one-index-off — do not copy a domain number
   from `status`; look it up in the grid dir (`4.05-data.md` → `--domain
diff --git a/tests/test_auger.py b/tests/test_auger.py
index 02a7c37..52d7d64 100644
--- a/tests/test_auger.py
+++ b/tests/test_auger.py
@@ -1565,6 +1565,160 @@ def test_ask_never_surfaces_a_question_with_an_unresolved_blocker(
     assert "Q-000002" in out and Q_STORE in out, out
 
 
+# ---------------------------------------------------------------- ask -> stores what it proposed (AUG-035)
+# The README's loop is `ask` then `answer --question-id`, and until AUG-035 the first half wrote
+# nothing: `ask` printed the question JEV picked and the `question` table stayed empty, so the
+# documented next move refused the engine's own suggestion. These cases assert the ROWS and the loop,
+# not the print.
+Q_NEXT = "how_is_it_tested"  # what JEV's new_question choice carries — the criterion it picked
+
+
+def ask_stub(choice: str, conf, noul):
+    """A JEV stub that answers `ask`'s four questions the way the live decisions model does."""
+
+    def _stub(state, questions, *a, **k):
+        answers = {
+            "next_subject": {"type": "choice", "choice": "testability", "confidence": 0.70},
+            "new_question": {"type": "choice", "choice": choice, "confidence": conf},
+            "completeness": {"type": "score", "score": 2, "confidence": 0.70},
+        }
+        if noul is not None:
+            answers["already_answered"] = {"type": "noul", "noul": noul}
+        return {"answers": answers, "usage": {"cost": 1e-05}, "model": "stub"}, None
+
+    return _stub
+
+
+def test_ask_stores_the_question_it_proposes_and_the_answer_then_closes_it(
+    project: dict, monkeypatch
+):
+    """The loop, end to end on the rows: ask stores the proposal, answer --question-id closes it."""
+    ns, pid = project["ns"], project["pid"]
+    monkeypatch.setattr(auger, "jev", ask_stub(Q_NEXT, 0.73, 0.10))
+
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0, out
+
+    # The print contract is intact and the one line AUG-035 adds says the question is now closable.
+    assert f"next question: {Q_NEXT}  (0.73)" in out, out
+    assert "genuinely new, ask it" in out, out
+    assert "stored question: Q-000001" in out, out
+
+    q = row(ns, "question", "id=eq.Q-000001")
+    assert q["project_id"] == pid and q["text"] == Q_NEXT, q
+    assert q["status"] == "open", q
+    assert q["qclass"] == auger.ASK_CLASS, q  # the source marker: this one came from ask
+    assert q["ring"] == 1 and q["domain"] == "", q
+    # The JEV value the table declares a column for: the gate's answered-verdict, and when it was taken.
+    assert q["jev_already_answered"] == pytest.approx(0.10), q
+    assert q["jev_checked_at"], q
+    assert len(rows(ns, "question", f"project_id=eq.{pid}")) == 1
+
+    facets = rows(ns, "facet", "question_id=eq.Q-000001&order=id.asc")
+    assert [f["facet"] for f in facets] == list(auger.facet_set()), facets
+    assert all(f["status"] == "open" for f in facets), facets
+
+    # The loop's central move: the command the README documents no longer refuses.
+    rc, out = answer_cli(ns, "D-001", "4.05", "single SQLite file", qid="Q-000001")
+    assert rc == 0, out
+    assert "closed Q-000001" in out, out
+    assert row(ns, "decision", "id=eq.D-001")["question_id"] == "Q-000001"
+    assert row(ns, "question", "id=eq.Q-000001")["status"] == "answered"
+    facets = rows(ns, "facet", "question_id=eq.Q-000001")
+    assert all(f["status"] == "closed" and f["closed_by"] == "D-001" for f in facets), facets
+
+
+def test_ask_does_not_store_the_same_question_twice(project: dict, monkeypatch):
+    """A second ask naming the same question says it is already open instead of storing it again."""
+    ns, pid = project["ns"], project["pid"]
+    monkeypatch.setattr(auger, "jev", ask_stub(Q_NEXT, 0.73, 0.10))
+
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0 and "stored question: Q-000001" in out, out
+
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0, out
+    assert "stored question:" not in out, out
+    assert "question not stored: already open as Q-000001" in out, out
+    assert len(rows(ns, "question", f"project_id=eq.{pid}")) == 1
+    assert len(rows(ns, "facet", "question_id=eq.Q-000001")) == len(auger.facet_set())
+
+
+@pytest.mark.parametrize(
+    "stub, want",
+    [
+        (ask_stub(Q_NEXT, 0.30, 0.10), f"the proposal is below threshold {auger.T_SUBJECT}"),
+        (ask_stub(Q_NEXT, 0.73, auger.T_ANSWERED + 0.05), "the gate scores it already answered"),
+        (ask_stub(Q_NEXT, 0.73, None), "the gate returned no answered-verdict"),
+        (ask_stub("", 0.73, 0.10), "JEV proposed no question"),
+    ],
+)
+def test_ask_stores_nothing_when_a_gate_refuses_the_proposal(
+    project: dict, monkeypatch, stub, want
+):
+    """Fail-closed: every refusal leaves the `question` table exactly as empty as it was, and says so."""
+    ns, pid = project["ns"], project["pid"]
+    monkeypatch.setattr(auger, "jev", stub)
+
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0, out
+    assert f"question not stored: {want}" in out, out
+    assert "stored question:" not in out, out
+    assert rows(ns, "question", f"project_id=eq.{pid}") == []
+    assert rows(ns, "facet", f"project_id=eq.{pid}") == []
+
+
+def test_ask_stays_a_report_when_the_store_itself_is_refused(project: dict, monkeypatch):
+    """A refused insert stores nothing, is printed with the transport's own message, and keeps rc 0."""
+    ns, pid = project["ns"], project["pid"]
+    monkeypatch.setattr(auger, "jev", ask_stub(Q_NEXT, 0.73, 0.10))
+
+    def refuse(*a, **k):
+        raise SystemExit("insert question failed (400): boom")
+
+    monkeypatch.setattr(auger, "insert", refuse)
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0, out
+    assert "question not stored: the question row was refused" in out, out
+    assert rows(ns, "question", f"project_id=eq.{pid}") == []
+
+
+def test_ask_reports_a_partial_store_instead_of_calling_it_clean(project: dict, monkeypatch):
+    """The row and its facets are two writes. A facet that fails after the row landed is SAID OUT LOUD.
+
+    Nothing here can be undone — the module has no row delete — so the contract is that a partial
+    store is reported as one, never printed as `stored question:` and left to look complete.
+    """
+    ns = project["ns"]
+    monkeypatch.setattr(auger, "jev", ask_stub(Q_NEXT, 0.73, 0.10))
+    real, last = auger.facet, auger.facet_set()[-1]
+
+    def half(ns_: str, pid_: str, qid_: str, name: str, **k):
+        if name == last:
+            raise SystemExit("insert facet failed (400): boom")
+        return real(ns_, pid_, qid_, name, **k)
+
+    monkeypatch.setattr(auger, "facet", half)
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 0, out
+    assert "stored question: Q-000001" in out, out
+    assert f"WARNING the question row landed but facet {last!r} did not" in out, out
+    assert len(rows(ns, "facet", "question_id=eq.Q-000001")) == len(auger.facet_set()) - 1
+
+
+def test_ask_stores_nothing_when_jev_is_unreachable(project: dict, monkeypatch):
+    """The pre-existing fail-closed arm is unchanged: no verdict, no store, exit 1 and no row."""
+    ns, pid = project["ns"], project["pid"]
+    monkeypatch.setattr(
+        auger, "jev", lambda *a, **k: (None, "all 6 JEV keys failed; last: HTTP 401")
+    )
+    rc, out = run_cli(["-n", ns, "ask"])
+    assert rc == 1, out
+    assert "JEV unavailable" in out, out
+    assert "stored question:" not in out and "question not stored" not in out, out
+    assert rows(ns, "question", f"project_id=eq.{pid}") == []
+
+
 def test_propagate_links_a_question_the_gate_can_answer_after_its_parent_closes(
     project: dict, monkeypatch
 ):
