diff --git a/.coding-hermes/board/board.jsonl b/.coding-hermes/board/board.jsonl
index e79eff4..b587f93 100644
--- a/.coding-hermes/board/board.jsonl
+++ b/.coding-hermes/board/board.jsonl
@@ -1 +1 @@
-{"project":"auger","namespace":"coding-hermes","version":1,"ticks_total":7,"ticks_idle":0,"cooldown_s":21600,"last_commit":"64e076a","updated_at":"2026-09-23 07:05:00"}
+{"project": "auger", "namespace": "coding-hermes", "version": 1, "ticks_total": 7, "ticks_idle": 0, "cooldown_s": 21600, "last_commit": "64e076a", "updated_at": "2026-09-23 03:07:11.000000", "last_tick": "2026-09-23 03:07:11.000000"}
diff --git a/.coding-hermes/board/events.jsonl b/.coding-hermes/board/events.jsonl
index 2abb5de..e052410 100644
--- a/.coding-hermes/board/events.jsonl
+++ b/.coding-hermes/board/events.jsonl
@@ -90,3 +90,4 @@
 {"id": 90, "timestamp": "2026-09-23 07:00:48", "event_type": "task_dispatched", "task_id": "AUG-031", "actor": "foreman", "detail": "wave dispatch tick auger-2026-09-23-06-40-02; wt/AUG-031 @ 64e076a; brief /tmp/auger_20260923_064002_brief_AUG-031.txt", "tick_number": null}
 {"id": 91, "timestamp": "2026-09-23 07:13:48", "event_type": "task_completed", "task_id": "AUG-023", "actor": "foreman", "detail": "{\"status\":\"complete\"}", "tick_number": null}
 {"id": 92, "timestamp": "2026-09-23 07:13:48", "event_type": "task_completed", "task_id": "AUG-024", "actor": "foreman", "detail": "{\"status\":\"complete\"}", "tick_number": null}
+{"id":93,"timestamp":"2026-09-23 03:07:11.000000","event_type":"audit","task_id":null,"actor":"foreman","detail":"{\"summary\": \"dogfood run 3 (registers + ask-answer seam): full documented loop driven live (init/start/ask/answer/check/status/toggle/dump/propagate/feedback/recall/verdict), 2 P1 + 4 P2 findings filed as AUG-034..039, all re-verified at merged HEAD 8210d38; fresh-install leg re-run post-AUG-033 (bunker las-bunker-03 agent 4b004b7a destroyed; substrate build 72s, drill loop green, smoke 19/21 with both fails = missing OpenRouter key per documented requirement; control-host smoke 21/21); control CI green. Report docs/dogfood/2026-09-23-registers-seam-integration.md; diagnostics + usage skill updated.\", \"verdict\": \"PROMISING-BUT-ROUGH (registers + seam surface)\", \"install_seconds\": 72, \"bunker\": \"las-bunker-03 agent 4b004b7a destroyed\", \"smoke\": \"19/21 fresh (both fails=missing OR key, documented) + 21/21 control\"}","tick_number":20260923}
diff --git a/.coding-hermes/board/tasks.jsonl b/.coding-hermes/board/tasks.jsonl
index b0e3b79..73a2169 100644
--- a/.coding-hermes/board/tasks.jsonl
+++ b/.coding-hermes/board/tasks.jsonl
@@ -34,3 +34,9 @@
 {"id": "AUG-031", "title": "[P2] docs drift: README lists 13 tables (verdict missing), VERBS.md header lists 11 verbs (verdict missing), and verdict's --config summary semantics (HYPOTHETICAL renders still summarize the ACTIVE CONFIGURATION line) are documented nowhere", "status": "in_progress", "priority": "P2", "type": null, "created_at": "2026-09-23T04:45:00+00:00", "created_by": null, "description": null, "acceptance_criteria": "Dogfood 2026-09-23: README 'The tables' block says COLS is the source of truth then contradicts it (13 names, verdict absent \u2014 the table landed in c8c32fa/AUG-003 after the README was written). VERBS.md L11 verb index: 'init start ask answer check status toggle dump propagate feedback recall' \u2014 verdict's own entry exists but the index and the count lie. Also undocumented: (a) `verdict --ask-jev --config X=Y` records a config_summary quoting the ACTIVE CONFIGURATION line of the HYPOTHETICAL render (the summary names every decision, so --list output is enormous and the word 'ACTIVE' is wrong for a hypothesis \u2014 V-000002's stored summary says 'ACTIVE CONFIGURATION: ... D-003=keep v1...' which is NOT the active set); (b) --config values accept option ids OR labels (both proven); (c) a verdict row with no config (bare --good) stores an empty summary \u2014 --list shows 'config: \u2014' and '{}' where confidence formatting should be. ACCEPTANCE: README table list matches len(COLS)=14 with verdict described; VERBS.md index line includes verdict; verdict entry documents the config_summary behavior incl. the hypothesis-mode wording; decide and fix whether hypothesis summaries should say HYPOTHETICAL CONFIGURATION.", "source": "dogfood 2026-09-23 (graph/bundles/verdict surface)", "review_notes": null, "capability_tags": ["dogfood", "bundles", "docs"], "complexity": 2, "attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "wave auger-2026-09-23-06-40-02: worktree /home/kara/worktrees/auger-AUG-031, branch wt/AUG-031, base 64e076a; worker deepseek-v4-flash@deepseek-foreman", "guard_result": null, "ci_result": null, "commit_hash": null, "completed_at": null, "lines_added": null, "lines_removed": null, "perpetual": null, "primary_model": null, "primary_provider": null, "reasoning": null, "updated_at": "2026-09-23T07:00:48+00:00", "worker_status": null, "worker_summary": null}
 {"id": "AUG-032", "title": "[P3] PERF: `auger recall` cold start 25s vs warm 2s \u2014 first search a user runs pays a 12x penalty; embedding-provider RTT dominates, not compute", "status": "pending", "priority": "P3", "type": null, "created_at": "2026-09-23T04:45:00+00:00", "created_by": null, "description": null, "acceptance_criteria": "Command: `python3 auger.py -n mccli recall \"how much event history do we keep?\" --limit 4` \u2014 cold 25s (first call in the namespace's process lifetime), warm 2s (second query, `recall \"storage retention window\" --limit 3`). ~23s delta = embedding of the QUERY + substrate cold path, not retrieval: warm recall over 12 embedded memories is 2s. A user's first 'recall' is exactly when they judge the tool. Not a blocker (25s is tolerable once), but worth (a) a one-line docs note ('first recall embeds the query; expect ~20s'), (b) if trivial, an embed-cache or a printed 'embedding query...' progress line so 25s does not read as a hang. No profile taken \u2014 the number is wall-clock from one cold + one warm run, 2026-09-23.", "source": "dogfood 2026-09-23 (graph/bundles/verdict surface)", "review_notes": null, "capability_tags": ["dogfood", "bundles", "perf"], "complexity": 1, "attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Measure-first law: if you touch this, re-measure with the same command before/after. hyperfine unnecessary \u2014 the cold/warm split IS the signal.", "guard_result": null, "ci_result": null, "commit_hash": null, "completed_at": null, "lines_added": null, "lines_removed": null, "perpetual": null, "primary_model": null, "primary_provider": null, "reasoning": null, "updated_at": "2026-09-23T04:45:00+00:00", "worker_status": null, "worker_summary": null}
 {"id": "AUG-033", "title": "[P2] fresh-install quickstart needs 3 undocumented steps: pnpm bootstrap (standard routes hit root walls on Debian), mkdir for DUCKBRAIN_DATA_DIR (ENOENT pidfile), and AUGER_DOMAIN_GRID is unset-able on a box without the skill tree", "status": "complete", "priority": "P2", "type": null, "created_at": "2026-09-23T05:05:00+00:00", "created_by": null, "description": null, "acceptance_criteria": "Bunker fresh-install leg 2026-09-23 (las-bunker-03, agent 864a37ab, bare Debian 13 user, node 22 preinstalled): (1) README's `pnpm install && pnpm build` 127s \u2014 pnpm missing; `corepack enable pnpm` dies EACCES (symlinks into /usr/bin), `npm i -g pnpm` dies EACCES (writes /usr/lib) \u2014 the working path is `npm config set prefix ~/.npm-global && export PATH=~/.npm-global/bin:$PATH && npm i -g pnpm` (5s, pnpm 12.4.2), after which install+build are green in ~76s total. (2) `DUCKBRAIN_DATA_DIR=~/dd-data node bin/duckbrain.js http` dies ENOENT writing the pidfile unless the dir pre-exists: 'mkdir -p ~/dd-data' first \u2014 the error names the pidfile, not the missing dir. (3) `init --seed-domains` on a fresh box refuses with the skill-tree path (good, loud) \u2014 but the README quickstart runs it bare; a fresh user without the spec-decomposition skill needs AUGER_DOMAIN_GRID pointed at any canonical 44-domain grid, which the quickstart doesn't say. ACCEPTANCE: README quickstart gains a 'first: bootstrap pnpm (user prefix), mkdir the data dir' block OR the install docs link a setup script; each step's failure mode quoted above gets one line in docs/dogfood/diagnostics.md; re-run the bunker leg clean and quote the new total.", "source": "dogfood 2026-09-23 (bunker install leg, re-run after eebdac3)", "review_notes": null, "capability_tags": ["docs", "install", "fresh-machine"], "complexity": 2, "attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "merged wt/AUG-033 (9292584, merge 64e076a); yaml lifecycle complete 06:34Z; CI run success 06:34", "guard_result": "PASS", "ci_result": "GREEN", "commit_hash": "9292584", "completed_at": "2026-09-23 07:05:00", "lines_added": 77, "lines_removed": 0, "perpetual": null, "primary_model": null, "primary_provider": null, "reasoning": null, "updated_at": "2026-09-23 06:54:26", "worker_status": "done", "worker_summary": "quickstart bootstrap block (pnpm user-prefix, data dir, AUGER_DOMAIN_GRID) + diagnostics failure modes", "worktree": "/home/kara/worktrees/auger-AUG-033", "branch": "wt/AUG-033", "sessions": ["auger-2026-09-23-04-53-05"]}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-034", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 1, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P1] answer --question-id refusal is NOT atomic: decision+options stored BEFORE close_question refuses; retry duplicates the decision id", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "cmd_answer inserts the decision row and option rows, THEN close_question() raises 'refused: question does not exist' (correct refusal, wrong ORDER). User sees 'refused', assumes nothing was stored, re-runs without --question-id \u2014 a second D-001 row is created (decision ids not unique-keyed; nothing stops it). Live damage in auger-df-registers: D-001 x2, option count 6, dump rendered the block twice, ACTIVE CONFIGURATION and its contradictions each x4. Repro at 8210d38: `python3 auger.py -n auger-df-registers answer --id D-004 --question-id Q-does-not-exist ...` -> rc=1 refused, yet GET /api/ns/<ns>/tables/decision?id=eq.D-004 returns the full row. Fix: validate the question BEFORE the decision insert (node_exists first), and a unique key on decision.id."}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-035", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 1, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P1] ask->answer seam broken: ask never persists the question it proposes; answer --question-id then refuses, and no verb stores questions", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "README loop: ask -> 'JEV says what to drill next' -> answer. In use (auger-df-registers, 2026-09-23): ask printed 'next question: how_is_it_tested (0.73)' and stored NOTHING (question table empty after the call, verified over HTTP). answer --question-id Q-how_is_it_tested refused (correct, close_question by design) \u2014 but no verb can store the question, so the proposed question can NEVER be closed except by hand-written HTTP. Only writer of question rows is feedback (thin decisions <0.6 only). A user following the README cannot complete the loop's central move. Fix: ask --commit (or default) stores the proposed question with its JEV confidence + facet rows, then answer --question-id works as documented."}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-036", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P2] toggle --on <bare-option-suffix> exits 0 'toggled ... (0)' and patches ZERO rows \u2014 silent no-op", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "`toggle --on O2` printed 'toggled: O2->on (0)' and exited 0; dump showed 'ACTIVE CONFIGURATION: (nothing active)'. The (0) is the only hint (patch count). Full id form works: 'toggle --on D-001-O2' -> (2) rows. A bare suffix must resolve or be refused by name; rc=0 + 'toggled' is a silent no-op that looks like a write. Fix: resolve the bare suffix against the decision's options, or refuse: 'no option O2 \u2014 use the full id D-00X-OY'."}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-037", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P2] dump --config help advertises 'D-001=O2'; the parser discards that shorthand as unparsed at rc=0", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "`dump --help` shows '--config D-001=O2'; real parse requires the full option id (D-001=D-001-O2) or an option LABEL; the documented form is silently dropped ('WARNING: unparsed --config entries ignored: D-001=O2 (no such option for D-001)') at rc=0 \u2014 the rendered hypothesis is silently the CURRENT config, not the one the user asked for. (Run-2 report re-checked dump with a full id and cleared it; the SHORTHAND from the help string is what fails.) Fix: parse the documented shorthand, or fix the help string."}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-038", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P2] answer --domain accepts any string (4.99 recorded); no verb maps domain names to numbers; status domain-coverage display is one-index-off", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "answer --domain 4.99 recorded silently at HEAD; and a real decision landed on 4.27 (audio) because the user believed 4.27=controls \u2014 the verb never maps the 44 grid names to numbers. status makes it worse: D-002 (4.05) displayed under the 4.27 row, so the number a user copies is wrong twice (display AND acceptance). Repro: `answer --id D-005 --domain 4.99 --chosen x` -> recorded. Fix: validate --domain against the seeded grid/namespace domain rows; print the name in answer's confirmation; fix the off-by-one display in status."}
+{"attempts": 0, "blocked_reason": null, "blocked_since": null, "blocks": [], "capability_tags": ["engine", "cli"], "ci_result": null, "commit_hash": null, "completed_at": null, "complexity": 1, "created_at": "2026-09-23 08:15:00", "depends_on": [], "dispatched_at": null, "exit_code": null, "fallback_model": null, "fallback_provider": null, "files_changed": [], "foreman_note": "Bane design call: wire the three registers as verbs vs cut them from COLS+status. Foreman stance: cutting dead registers is smaller and more honest; answer --assumption/--breaks-what flags are the middle path.", "guard_result": null, "id": "AUG-039", "lines_added": null, "lines_removed": null, "perpetual": false, "primary_model": null, "primary_provider": null, "priority": 2, "reasoning": "dogfood 2026-09-22 real-use run", "review_notes": null, "status": "pending", "title": "[P2] README token claim false on a fresh box: ~/.duckbrain/foreman-status.token is never created by a fresh boot; first auger command dies 'no DuckBrain token'", "updated_at": "2026-09-23 08:15:00", "worker_status": null, "worker_summary": null, "detail": "README 'Requirements' says DuckBrain 'with its token at ~/.duckbrain/foreman-status.token'. Fresh install leg (bunker las-bunker-03, agent 4b004b7a, 2026-09-23): after documented substrate boot, ~/.duckbrain/ does not exist (daemon auth default=none; keys would live in ~/.duckbrain/auth.json only in apikey mode). Following the README verbatim, `auger init` exits 'no DuckBrain token: set DUCKBRAIN_API_KEY or ~/.duckbrain/foreman-status.token' \u2014 with no documented step that creates either. DUCKBRAIN_API_KEY=dummy works (auth=none) but is undiscoverable from docs. Fix: README one-liner for the env var (and where real keys come from). Note: skill skills/auger-usage pitfall 5 already documents the working pairing; the README is the gap."}
diff --git a/.coding-hermes/dogfood-log.md b/.coding-hermes/dogfood-log.md
index 8bd4eac..975f596 100644
--- a/.coding-hermes/dogfood-log.md
+++ b/.coding-hermes/dogfood-log.md
@@ -4,3 +4,4 @@
 |---|---|---|---|---|---|
 | 2026-09-22 | ✅ SHIPPABLE (with substrate caveat) | "A user can drill a project into a git-backed spec of choices via an 11-verb CLI backed by DuckBrain+JEV" — core loop holds: init/start/ask/answer/check/toggle/dump all correct and fast on the working substrate; the documented fresh-install pairing is broken (README + duckbrain default branch lack the declared-tables API). | AUG-016 (P0 fresh-user pairing broken: duckbrain default branch has no tables API — README must pin feat/native-s3) · AUG-015 (P1 toggle allows two active options per decision; dump renders both as the config) · AUG-017 (P1 live namespace edge declaration drifts from code; cross-project fields silently dropped; one init repairs) | ~90s on control box; fresh box: broken as documented, ~4 min once branch pinned | fresh leg ~240s incl. npm (bunker las-bunker-03, agent cab82db4, destroyed; smoke=ok on branch) |
 | 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (graph/bundles/verdict surface) | "A user can run the graph walks (propagate), the feedback engine, bundles (SPEC-002) and the verdict record" — the graph engine, feedback/gate loop and verdict verb WORK and are excellent; the bundle layer is invisible when it runs (silent 0.24-confidence 'unchanged' honored, AUG-027) and half-unreachable (no verb writes the moot trigger, AUG-028; no verb creates membership, AUG-029). | AUG-027 (P1 bundle impact pass silent + no confidence floor) · AUG-028 (P1 moot cascade reachable only by hand-written edges) · AUG-029 (P2 no bundle verbs + scheduler-DB-gated membership + namespace convention documented nowhere) · AUG-030 (P2 no supersession; JEV itself judged the contradictory active config BAD 0.36) · AUG-031 (P2 docs drift: 13-table/11-verb lists miss verdict) · AUG-032 (P3 recall cold 25s vs warm 2s) · AUG-033 (P2 fresh-install: 3 undocumented steps) | first real success (feedback → real question) 17s after setup; setup cost ~10 min incl. a namespace-convention mis-pair the docs caused | fresh leg re-run: auger loop 7s zero-install; substrate 3 undocumented steps then ~85s (bunker las-bunker-03, agent 864a37ab, destroyed; smoke=ok) |
+| 2026-09-23 | 🟡 PROMISING-BUT-ROUGH (registers + ask→answer seam) | "A user can record breaks/assumptions/unknowns and complete the README's own ask→answer loop" — read/derive verbs (check/status/dump/verdict/recall/propagate/feedback) are excellent and fast; the WRITE surface breaks the loop: ask persists nothing (AUG-035), answer's refusal leaves half a write behind and the retry duplicates the decision id (AUG-034), toggle/dump help-text contracts lie (AUG-036/037), --domain unvalidated (AUG-038); registers remain verb-less (AUG-019, confirmed twice by use). Fresh-install leg re-run post-AUG-033: README bootstrap accurate, one token gap remains (AUG-039). | AUG-034 (P1 non-atomic refusal + duplicate id) · AUG-035 (P1 ask→answer seam broken) · AUG-036/037/038/039 (P2 contract drifts) | ~12 min to first real verdict (verdict --ask-jev judged my what-if BAD, noul 0.07); setup 3s init + 1s start | fresh leg 72s substrate build + 6s smoke 19/21 (fails=missing OR key); bunker las-bunker-03, agent 4b004b7a, destroyed; smoke=ok |
diff --git a/docs/dogfood/diagnostics.md b/docs/dogfood/diagnostics.md
index 1bf6c3f..6d2943e 100644
--- a/docs/dogfood/diagnostics.md
+++ b/docs/dogfood/diagnostics.md
@@ -131,3 +131,44 @@ working commands in the README quickstart path.
   canonical 44-domain grid (`4.01`–`4.44`), or skip `--seed-domains`; the loud
   refusal is right — a seeded 43 is exactly the silent-domain failure the rule
   forbids.
+
+## Registers + seam run (2026-09-23, third run — report:
+docs/dogfood/2026-09-23-registers-seam-integration.md)
+
+How the write surface actually behaves, as learned by driving a real drill
+(N100 backup policy, scratch namespace `auger-df-registers`):
+
+- **A decision insert precedes its own validation.** `cmd_answer` stores the
+  decision + options, THEN `close_question()` refuses a nonexistent
+  `--question-id`. The refusal is by design (the spec is right about why) —
+  but the order is wrong: a "refused" exit must mean nothing was stored.
+  Because decision ids are not unique-keyed, the natural retry then created a
+  duplicate id (dump rendered it twice; every derived count ×4).
+- **`ask` is a proposal, not a write.** Nothing it prints is persisted (the
+  `question` table stayed empty after a call that named a next question). The
+  only question-writer is `feedback` (thin decisions only), so the README's
+  own loop — ask, then answer `--question-id` — cannot complete for a
+  not-yet-stored question. Anyone reading `ask`'s output as "the engine
+  recorded the next question" is being set up for the non-atomic refusal
+  above.
+- **Two CLI contracts lie in their own help text.** `toggle --on O2` (bare
+  id) exits 0 with "toggled … (0)" and patches zero rows; `dump --config
+  D-001=O2` (the form `--help` shows) is discarded as unparsed. The full
+  id forms work; the discrepancy is discoverable only by diffing behavior
+  against the help string.
+- **`--domain` is free text.** `4.99` was accepted silently; a real decision
+  landed on 4.27 (audio) because the verb never maps the 44 grid names to
+  numbers and `status`'s domain-coverage display is one-index-off, so the
+  number a user copies is wrong twice.
+- **The substrate token the README names does not exist on a fresh box.**
+  `~/.duckbrain/foreman-status.token` is a host-convention artifact (this
+  control host has one because a previous deployment created it); a fresh
+  boot creates no `~/.duckbrain/` at all and runs auth=none.
+  `DUCKBRAIN_API_KEY=<any value>` satisfies auger's env-var read — the
+  README just never says so.
+- **Verified good, unchanged from run 2:** JEV's fail-closed behavior (fresh
+  box without a key: check exits rc=1 with "UNKNOWN, not 'already
+  answered'" — exactly the documented stance), the what-if hypothesis
+  (nothing written, contradictions quoted from the record), verdict's
+  record-then-judge flow, and the smoke suite's own hygiene (namespace
+  sweep/teardown ran clean on the bunker).
diff --git a/skills/auger-usage/SKILL.md b/skills/auger-usage/SKILL.md
index c63f44b..8a8631d 100644
--- a/skills/auger-usage/SKILL.md
+++ b/skills/auger-usage/SKILL.md
@@ -31,7 +31,10 @@ only. Public repo: `coding-hermes/auger`.
 - DuckBrain HTTP on `127.0.0.1:3000` — **on the working branch
   `feat/native-s3` of wojons/duckbrain**; the public default branch has NO
   declared-tables API and auger's happy path 404s on it (AUG-016).
-- Credentials: `DUCKBRAIN_API_KEY` env or `~/.duckbrain/foreman-status.token`;
+- Credentials: `DUCKBRAIN_API_KEY` env or `~/.duckbrain/foreman-status.token`
+  — NOTE: a fresh DuckBrain boot creates NO token file (the substrate runs
+  auth=none by default; any non-empty `DUCKBRAIN_API_KEY` value works, see
+  pitfall 5; AUG-039 tracks the README drift);
   `DUCKBRAIN_URL` overrides the base URL; JEV needs an OpenRouter key in
   `~/.hermes/.env`.
 
@@ -54,11 +57,13 @@ only. Public repo: `coding-hermes/auger`.
 ## Common pitfalls (each one bit a real run)
 
 1. **Reading an error body as data.** `init` on a 429/401 body once "succeeded"
-   with 0 tables. If `init` prints `declared: 0/13` or an odd count, STOP —
+   with 0 tables. If `init` prints `declared: 0/14` or an odd count, STOP —
    the substrate is lying or unreachable; do not proceed to `start`.
-2. **toggle has no exclusivity (AUG-015)**: turning a second option ON leaves
-   the first ON, and `dump` then renders two bindings for one decision as THE
-   configuration. Until fixed, always pair `--on X --off Y` in one call.
+2. **toggle has per-decision exclusivity now (AUG-015 fixed)**: one option ON
+   deactivates its siblings by default; `--additive` opts back out. But use
+   the FULL option id (`D-001-O2`) — a bare `O2` exits 0 with "toggled …
+   (0)" and patches NOTHING (AUG-036). If a subsequent dump shows "nothing
+   active", this no-op is why.
 3. **JEV probe shape**: a probe missing the body's `model` field 400s on every
    key and looks like a total outage. Mirror `JEV_MODEL` from auger.py.
 4. **ask/check take 5-7s**: that is the JEV network round trip. Fail-closed
@@ -76,7 +81,31 @@ python3 auger.py -n <ns> status        # instant; proves API + token + tables
 python3 auger.py -n <ns> recall "any seed phrase"   # proves embeddings
 ```
 
-## Bundles and the graph (the 2026-09-23 surface)
+- **The moot cascade has a verb now (AUG-028 merged 2026-09-23):**
+  `answer --invalidates <decision-id> --invalidates-why "…"` records the
+  LOCAL `breaks` edge that drives `propagate`'s rule walk (dst_project
+  ABSENT = local; a sibling break still routes to an escalation). The old
+  hand-POSTed edge shape below still describes the storage truth.
+- **The registers have NO write verbs (AUG-019, confirmed twice by use).**
+  `break`/`assumption`/`unknown`/`escalation` rows are declared and read
+  (status counts unknowns; ask reads them) but no CLI command creates one —
+  only `answer --invalidates` (edges, not rows) and the propagate impact
+  pass (escalations) write anything. A seed that says "X is unknown" will
+  still show `unknowns: 0` until a row is hand-POSTed.
+- **`ask` does NOT persist its proposal (AUG-035).** The question it names
+  is never stored; `answer --question-id Q-…` on it will refuse (AUG-034's
+  non-atomic shape: the DECISION is stored BEFORE the refusal — do not
+  blindly retry without `--question-id`, or you duplicate the decision id;
+  decision ids are not unique-keyed yet). Until fixed: questions enter the
+  store only via `feedback` (thin decisions) or hand-POST.
+- **`--domain` is unvalidated free text (AUG-038)** and `status`'s
+  domain-coverage display is one-index-off — do not copy a domain number
+  from `status`; look it up in the grid dir (`4.05-data.md` → `--domain
+  4.05`).
+- **`dump --config` full ids only (AUG-037):** the help's `D-001=O2`
+  shorthand is discarded as "unparsed" at rc=0; write `D-001=D-001-O2`
+  (option labels also accepted per verdict, per the note above).
+- **Bundles and the graph (the 2026-09-23 surface)**
 
 - **One namespace per member, named exactly the member** (`mccli`, `mcview`).
   This convention is code-only (`member_namespace`, auger.py ~L865); a
@@ -90,10 +119,6 @@ python3 auger.py -n <ns> recall "any seed phrase"   # proves embeddings
   did not run; check the edge table.
 - **`propagate` is cheap and mostly a no-op on feedback-born questions**
   (feedback pre-gates them); `--recheck` forces real re-examination.
-- **The moot cascade needs a hand-written LOCAL `breaks` edge** (no verb
-  writes one, AUG-028). Shape: kind=breaks, src decision, dst decision,
-  dst_project ABSENT — with dst_project set it is a sibling break and the
-  local walk ignores it by design.
 - **Verdict shapes:** `verdict --good|--bad [--judged-by NAME]
   [--confidence X]` (on-file), `--ask-jev` (JEV judges the rendered dump,
   fail-closed), `--config D-001=O2` (hypothesis; values accept option ids OR
