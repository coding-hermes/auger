# Development-only pins for the pytest suite. The gate does NOT install from this file:
# `tests/gate.sh` runs `python3 -m pytest` with whatever the system python3 provides
# (pytest 9.1.1 here), so the gate has no venv dependency.
pytest>=8
